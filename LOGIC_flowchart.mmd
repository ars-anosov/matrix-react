flowchart LR
  UI["React: компоненты (глупые) + контейнеры"]

  subgraph Redux["Redux — view-model"]
    IDX["индекс комнат<br/>roomIds · selectedRoomId<br/>roomsMeta"]
    SES["сессия<br/>status · responseData<br/>deviceVerification"]
  end

  subgraph SVC["src/services"]
    CL["matrixClient<br/>session / crypto / sync / токены"]
    ROOMS["matrixRooms<br/>getRoomMeta / getRoomMessages<br/>watchRoomList / watchRoomMessages"]
  end

  SDK["matrix-js-sdk<br/>Room / Timeline / MatrixEvent<br/>(источник истины данных)"]
  Store[("local store<br/>LS + IndexedDB")]

  UI -->|"dispatch: login / restore / logout / select"| Redux
  Redux -->|"actions → методы сервиса"| SVC
  ROOMS -->|"дельта INITIALIZE / PUT / DELETE"| IDX
  CL -->|"session → status"| SES
  UI -->|"useRoomMessages → сообщения активной комнаты"| ROOMS
  CL <--> Store
  ROOMS -->|"чтение Room / Timeline"| SDK
  CL -->|"createClient / sync / crypto"| SDK
